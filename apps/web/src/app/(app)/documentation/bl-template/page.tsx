'use client';

import { BlTemplateScreen } from '@/components/doc/bl-template-screen';

/** Reached from the BL Draft tab, not the sidebar — docs/MODULE_DOCUMENTATION.md §3.5. */
export default function Page() {
  return <BlTemplateScreen />;
}
