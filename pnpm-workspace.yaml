packages:
  - apps/*
  - packages/*

# Postinstall build scripts are blocked by default in pnpm 10+.
# Only these are trusted to run.
allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  '@tailwindcss/oxide': true
  esbuild: true
  prisma: true
  sharp: true
  unrs-resolver: true
