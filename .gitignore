node_modules/
.pnpm-store/

# build output
dist/
.next/
out/
*.tsbuildinfo

# prisma generated client (regenerated by `pnpm db:generate`)
apps/api/src/generated/

# env — .env.example is committed, real values are not
.env
.env.*
!.env.example
!.env.production.example

# uploads (CLAUDE.md §2: local disk in dev)
storage/
uploads/

# logs / os / editor
*.log
npm-debug.log*
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
.claude/settings.local.json

# Prisma's agent skills are committed once under .agents/skills/.
# .claude/skills/ is just junctions/symlinks onto that — committing it would
# duplicate every file. See README "Agent skills" to relink after cloning.
.claude/skills/

# test output
coverage/
